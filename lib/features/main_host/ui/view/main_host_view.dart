part of 'main_host_page.dart';

class MainHostView extends StatelessWidget {
  const MainHostView({super.key});

  @override
  Widget build(BuildContext context) {
    return const MainHostContent();
  }
}