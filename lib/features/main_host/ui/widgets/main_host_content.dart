import 'package:flutter/material.dart';

class MainHostContent extends StatelessWidget {
  const MainHostContent({super.key});

  @override
  Widget build(BuildContext context) {
    // Start building your UI here!
    return const Center(child: Text('MainHost Content'));
  }
}