import 'package:flutter/material.dart';

class RestaurantContent extends StatelessWidget {
  const RestaurantContent({super.key});

  @override
  Widget build(BuildContext context) {
    // Start building your UI here!
    return const Center(child: Text('Restaurant Content'));
  }
}